import React from "react";

function Button(){
    return(
        <div className="contact">
            <button type="button" className="contact-email">Email</button>
            <button type="button" className="contact-linkedin">LinkedIn</button>
        </div>
    );
}

export default Button;